import 'package:flutter/material.dart';

class ResponsiveRecomend extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('responsive recomend'),
    );
  }
}
import 'package:flutter/material.dart';

class ResponsiveSavedListing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('responsive saved listing'),
    );
  }
}

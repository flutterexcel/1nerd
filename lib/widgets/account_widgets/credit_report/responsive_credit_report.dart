import 'package:flutter/material.dart';

class ResponsiveCreditReport extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('responsive credit report'),
    );
  }
}